apiVersion: tekton.dev/v1beta1
kind: PipelineResource
metadata:
  name: image-registry
spec:
  type: image
  params:
    - name: url
      value: image-registry.openshift-image-registry.svc:5000/demo/test-square-example:latest