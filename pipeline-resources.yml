apiVersion: tekton.dev/v1beta1
kind: PipelineResource
metadata:
  name: git-repo
spec:
  type: git
  params:
    - name: revision
      value: master
    - name: url
      value: https://github.com/Kalinins93/TestSquareExample.git